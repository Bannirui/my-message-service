server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    index  index.html;
    try_files $uri $uri/ /index.html;
  }

  # 前端代理后端API 在vue项目的prod中配置的根域名是prod-api 代理成后端域名
  location /prod-api/ {
    proxy_pass http://backend:8080/;
  }
}
